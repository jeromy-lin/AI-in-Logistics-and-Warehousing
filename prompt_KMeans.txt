請幫我產生一份可以直接複製到 Google Colab 執行的完整 Python 程式碼。

使用者會上傳 Excel 檔案，請使用 `files.upload()` 讀取檔案。

Excel 工作表名稱為：

**SKU資料輸入**

請注意：

* Excel 欄位名稱位於第 4 列，因此請使用 `header=3`。
* 不要使用固定檔名，請自動讀取上傳的 Excel。

請使用下列 8 個特徵進行 K-Means 分群：

* 每日出貨箱數
* 每日揀貨次數
* 體積等級
* 重量等級
* 是否液體
* 是否易碎
* 是否季節性
* 目前距離主要作業區m

請將 K-Means 固定分成 **5 群**。

請注意資料前處理：

1. 體積等級（小、中、大）請轉成 1、2、3。
2. 重量等級（輕、中、重）請轉成 1、2、3。
3. 是否液體、是否易碎、是否季節性（是、否）請轉成 1、0。
4. 使用 `pd.to_numeric(errors="coerce")` 避免文字造成錯誤。
5. 缺失值請使用中位數補值。
6. 使用 `StandardScaler` 進行標準化。

完成 K-Means 後，請使用 PCA 將資料降至二維（X、Y），並繪製分群散布圖。

要求如下：

* 圖片全部使用英文。
* 表格全部使用中文。
* 圖片背景白色。
* 配色柔和、低飽和度。
* 每個群組使用不同顏色，並標示群中心。

請輸出以下結果：

1. SKU 分群結果表。
2. 五群統計摘要表。
3. PCA 分群圖。
4. 每群 SKU 數量。
5. 每群平均每日出貨箱數。
6. 每群平均每日揀貨次數。
7. 每群平均距離。

另外請進行簡單效益分析。

以：

**每日揀貨次數 × 目前距離主要作業區m**

作為距離成本。

建議將高出貨、高揀貨商品配置到距離較近的位置。

請計算：

* 改善前距離成本
* 改善後距離成本
* 節省距離成本
* 改善率（%）

並繪製改善前後比較長條圖。

最後請以中文輸出分析結論，包括：

* 共分析多少項 SKU
* 五群特性
* 建議優先調整哪些群組
* 每日可降低多少距離成本
* 每月（22 天）預估效益
* 每年（264 天）預估效益

最後請輸出 Excel：

**KMeans_SKU_分析結果.xlsx**

請只輸出完整、可直接執行的 Python 程式碼，不要提供任何解說。
